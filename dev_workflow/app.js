/**
 * Using Node Package Manager.
 * 
 * Run [npm init] to setup the package.json file.
 * Run [npm install --save] to add third party packages as production dependency.
 * Run [npm install --save-dev] to add third party packages as development dependency.
 * Run [npm install -g] to add the package to the machine so you can use it globally.
 * Browse [npmjs.com] to get documentation of packages.
 */